# Pinned, known-working dependency versions for reproducible installs.
#
# Generated from `pip list --format=freeze` against the environment this
# project has been developed and tested against - nothing here was upgraded,
# this is exactly what's already installed and working.
#
# Does NOT include `adpo-backend` / `adpo-analysis-engine` themselves (those
# are the local, editable packages - install with `pip install -e ../analyzer`
# and `pip install -e .` as documented in README.md) or `pip` (the installer
# itself, not a dependency).
#
# This covers the full [dev] extra too (pytest, pytest-cov, respx, and their
# transitive deps), not just the runtime-only dependency set, since that's
# what this environment has actually been tested with end to end. Trimming
# it to a runtime-only subset is a reasonable follow-up but risks silently
# dropping something a "dev-only" package turns out to share a transitive
# dependency with - left as-is intentionally for now.
#
# Usage: pip install -r requirements-lock.txt
#
# Regenerate by re-running `pip list --format=freeze` in a known-good venv
# and updating this file by hand - do not regenerate by blindly upgrading
# packages.

Mako==1.4.1
MarkupSafe==3.0.3
PyJWT==2.13.0
PyYAML==6.0.3
Pygments==2.21.0
SQLAlchemy==2.0.52
alembic==1.19.1
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
cffi==2.1.1
click==8.4.2
colorama==0.4.6
coverage==7.15.4
cryptography==50.0.0
fastapi==0.141.1
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
itsdangerous==2.2.0
packaging==26.3
pluggy==1.6.0
psycopg==3.3.4
psycopg-binary==3.3.4
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
pytest==9.1.1
pytest-cov==7.1.0
python-dotenv==1.2.3
respx==0.23.1
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
tzdata==2026.3
uvicorn==0.52.4
watchfiles==1.2.0
websockets==17.0.1
